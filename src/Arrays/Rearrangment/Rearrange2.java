package Arrays.Rearrangment;

public class Rearrange2 {
    //Rearrange such that arr[i]>=arr[j] if i is even
    // arr[i]<=arr[j] if i is odd and i>j
    //Sort and then store in another array
    //For space optimisation jugaad can be done
}
